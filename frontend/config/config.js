export const apiConfig = {
  BASE_URL: "b", // Basis-URL deiner API
  endpoints: {
    banners: "/banners",        // z.B. für Banner-Daten
    products: "/products",      // z.B. für Produktliste
    categories: "/categories",  // z.B. für Kategorien
    auth: "/auth",              // z.B. für Login/Register
  },
};